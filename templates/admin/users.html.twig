{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                 <h1>Gestions membres</h1>
            </div>
            <div class="col-md-6">
                <h1><a href="{{ path('users_add') }}">ADD User</a></h1>
            </div>
        </div>
       
        <table class="table table-hover">
            <tr>
                <td>Username</td>
                <td>Email</td>
                <td>Modification</td>
                <td>Suppression</td>
            </tr>
             {% for user in users %}
                <tr>
                    <td>{{ user.username }}</td>
                    <td>{{ user.email }}</td>
                    <td><a href="">Modif</a></td>
                    <td><a href="">Supprim</a></td>
                </tr>
            {% endfor %}
        </table>
       
    </div>
    
{% endblock %}